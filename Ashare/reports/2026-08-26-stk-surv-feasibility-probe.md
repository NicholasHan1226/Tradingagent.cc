# 机构调研数据面（stk_surv）可行性探针记录（research_only）

- 日期：2026-08-26。性质：候选数据面纯标签计数探针；未计算任何收益、
  未冻结任何分桶、未占条件层面板编号。体例同 C14 涨跌停否决探针
  （`2026-08-25-c14-prelimit-feasibility-probe.md`）。

## 数据面确认

- 端点 `stk_surv` 经共享 HTTP 桥接可用（权限已探明）；字段：
  `ts_code/name/surv_date/fund_visitors/rece_place/rece_mode/rece_org/
  org_type/comp_rece`。
- 覆盖抽样：跟踪宇宙（top-1000 主板扩展表）随机 50 只（seed=42），
  20240101–20260823 窗口逐只拉取：

| 口径 | 数值 |
|---|---|
| 有记录标的 | **39/50（78%）** |
| 总行数 | 3614（≈72 条/只/2.6 年，密度高） |
| 单只最早记录起始年 | 2024×28、2025×5、2026×6 |
| 记录跨度 | 20240103 – 20260817 |

- 个股例外如实入档：600519.SH 同窗口返回 0 行（覆盖不均匀的旁证，
  未来任何管道必须容忍单标的全缺档）。
- 字段质量警示：`fund_visitors`/`org_type` 大量落 `--`（稀疏）；
  可靠信号主要是 `surv_date` 计数强度与 `rece_mode`/`rece_org` 文本类。

## 关键边界：历史深度止于 2024 年初

28/50 只的最早记录恰为 20240103，指向端点级历史起点 ≈2024-01。对条件
层面板用途（跟踪器滚动验证窗 20260401 起）覆盖充足；但对更早的事前基线
研究不可用。任何未来假设绑定的回看段上限约两年半。

## 机制候选（仅记录，不冻结）

解禁前机构调研强度 = 信息不对称吸收/派发的事前关注度维度；与注意力文献
（散户注意力放大漂移、机构注意力相反）方向存在张力，正是值得检验处。
未来立项前需论证为何不被既有 parked 条目覆盖，再走独立预注册。

## 结论与后续

数据面**存活**（对照 C14 的否决判定）。但在候选台账修订版（#560，进行
中）合入前不认领面板编号、不做假设绑定冻结；台账落定后若仍无既有条目
冲突，按标准链路（预注册→引擎→读数）提请下一班次立项。

## 时点声明

本文档只含覆盖率与字段可用性事实；先于任何调研强度分桶收益计算合入主
线。
