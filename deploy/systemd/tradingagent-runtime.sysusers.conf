# Dedicated non-login identity. Install as
# /etc/sysusers.d/tradingagent-runtime.conf and run systemd-sysusers before
# installing any TradingAgent unit or runtime credential.
g tradingagent -
u tradingagent -:tradingagent "TradingAgent service identity" /nonexistent /usr/sbin/nologin
